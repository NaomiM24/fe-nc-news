import React from "react";
import ArticlesList from "./ArticlesList";

const Home = () => {
  return (
    <main className="homepage">
      <ArticlesList />
    </main>
  );
};

export default Home;
